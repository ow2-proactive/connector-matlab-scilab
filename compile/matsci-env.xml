<?xml version="1.0" encoding="UTF-8"?>
<project basedir="compile">

    <property environment="env" />

    <taskdef name="if" classname="ise.antelope.tasks.IfTask" classpath="${basedir}/lib/AntelopeTasks_3.2.19.jar"/>

    <!-- junit properties -->
    <property name="junit.showoutput" value="true" />

    <property name="base.dir" value="${basedir}/.." />
    <property name="lib.dir" value="${base.dir}/lib" />
    <property name="dev.dir" value="${base.dir}/dev" />
    <property name="proactive.jar" value="${lib.dir}/scheduling/ProActive.jar" />
    <property name="proactive.utils.jar" value="${lib.dir}/scheduling/ProActive_utils.jar" />

    <property name="compile.dir" value="${base.dir}/compile" />
    <property name="classes.dir" value="${base.dir}/classes" />
    <property name="junit.dir" value="${base.dir}/junitReports" />

    <property name="deploy.dir" value="${base.dir}/dist" />
    <property name="deploy.lib.dir" value="${deploy.dir}/lib" />




    <!-- ================================================================ -->
    <!--                   		Source paths                              -->
    <!--  =============================================================== -->

    <property name="src.dir" value="${base.dir}/src" />
    <property name="src.matsci.dir" value="${src.dir}/matsci/src" />
    <property name="src.matsci.tests.dir" value="${src.dir}/matsci/tests" />

    <!-- ================================================================ -->
    <!--                   		Class  paths                              -->
    <!--  =============================================================== -->

    <property name="cl.matsci.dir" value="${classes.dir}/matsci" />

    <property name="cl.matsci.tests.dir"    value="${classes.dir}/matsciTests" />

    <property name="proactive.path" value="org/ow2/proactive" />

    <property name="proactive.core.path" value="${proactive.path}/core" />
    <property name="proactive.ext.path" value="${proactive.path}/ext" />

    <!-- Javac properties -->
    <property name="debug" value="on" />
    <property name="source" value="1.5" />

    <!-- Clover -->
    <property name="clover.report.dir" value="${base.dir}/clover" />
    <property name="clover.historyreport.dir" value="${base.dir}/clover_history" />
    <property name="clover.historypoint.dir" value="${base.dir}/clover_db" />

    <property name="stubgenerator.class" value="org.objectweb.proactive.ext.util.StubGenerator" />

    <!-- ================================================================ -->
    <!--                    		jar  names                            -->
    <!--  =============================================================== -->

    <property name="matsci.jar"        value="ProActive_Matlab_Scilab.jar" />
    <property name="matsci.embedded.jar"        value="ProActive_Matlab_Scilab_Embedded.jar" />

    <!-- ================================================================ -->
    <!--                   define the different classpaths                -->
    <!--  =============================================================== -->

    <path id="matsci.libs">
        <fileset dir="${lib.dir}">
            <include name="matsci/**/*.jar" />
        </fileset>
    </path>

    <path id="all.libs">
            <fileset dir="${compile.dir}">
                <include name="lib/*.jar" />
            </fileset>
            <fileset dir="${lib.dir}">
                <include name="matsci/**/*.jar" />
            </fileset>
            <fileset dir="${lib.dir}">
                <include name="scheduling/**/*.jar" />
            </fileset>
        </path>





    <path id="matsci.classpath">
        <path refid="all.libs" />
        <pathelement location="${cl.matsci.dir}" />
    </path>

    <fileset id="jar.index.fileset" dir="${lib.dir}">
        <include name="scheduling/**/*.jar" />
        <include name="matsci/**/*.jar" />
    </fileset>


</project>