#INFORMATION : each file path must be absolute, OR relative to the Scheduler_Home path
#-------------------------------------------------------
#-------------   MATSCI PROPERTIES   ----------------
#-------------------------------------------------------

# Path to the Matlab Worker Configuration file (MatlabConfiguration.xml) which determines where Matlab is installed on a given machine
# pa.matsci.matlab.config.worker.local=

# Path to the Scilab Worker Configuration file (ScilabConfiguration.xml) which determines where Scilab is installed on a given machine
# pa.matsci.scilab.config.worker.local=

